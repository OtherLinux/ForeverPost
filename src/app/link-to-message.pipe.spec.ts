import { LinkToMessagePipe } from './link-to-message.pipe';

describe('LinkToMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new LinkToMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
